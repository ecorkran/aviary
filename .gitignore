# runtime / upstream — reproducible from scripts
server/
mindcraft/

# secrets — never commit
keys.json
*.key
.env

# noise
node_modules/
*.log

# disposable discovery spike code — only the marker is tracked
spikes/*
!spikes/README.md
